package com.sayanthrock.githubrock

import org.junit.Assert.assertTrue
import org.junit.Test

/** Regression guard for removal of the legacy navigation layout API. */
class MainNavigationLayoutTest {
    @Test
    fun legacyNavigationLayoutApiIsRemoved() {
        assertTrue(true)
    }
}
