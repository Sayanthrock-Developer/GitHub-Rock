name: Fix Markdown Compose Annotation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary repair workflow disabled after applying the fix."
